# Runtime dependencies for the Terracer Shiny app on Posit Connect Cloud.
#
# This is the RUNTIME subset only — the offline preprocessing pipeline
# (trimesh, rtree, lxml, httpx, tqdm, requests) is managed via pixi and is NOT
# needed to serve the app. geopandas reads the GeoJSON via pyogrio (its wheels
# bundle GDAL), so no system libraries are required.
#
# Pins below were resolved and validated end-to-end in a clean Python 3.12
# virtualenv (pip-only): tests/test_app.py passes and `shiny run src/app/app.py`
# serves successfully. Deploy on Connect Cloud with Python 3.12.
shiny==1.6.3
pandas==3.0.3
numpy==2.4.6
pyarrow==24.0.0
shapely==2.1.2
pyproj==3.7.2
geopandas==1.1.3
pyogrio==0.12.1
pysolar==0.13
pydeck==0.9.2
matplotlib==3.11.0
